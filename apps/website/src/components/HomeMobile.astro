---
import AppScreenshotCarousel from './AppScreenshotCarousel.astro';
import {
  STITCH_HOME_BENTO_CASSOULET_IMAGE,
  STITCH_HOME_BENTO_COURTYARD_IMAGE,
  STITCH_HOME_BENTO_STREET_ART_IMAGE,
  STITCH_HOME_CATEGORY_BALADE_IMAGE,
  STITCH_HOME_CATEGORY_CULTURE_IMAGE,
  STITCH_HOME_CATEGORY_GOUT_IMAGE,
  STITCH_HOME_HERO_MOBILE_IMAGE,
} from '../lib/stitch-home-assets';
import { APP_APK_DOWNLOAD_URL, SITE_CONTACT_EMAIL } from '../lib/site-config';

const categoryItems = [
  {
    label: 'Goût',
    icon: 'restaurant',
    borderClass: 'border-soft-pink',
    backgroundClass: 'bg-primary-container/20',
    iconClass: 'text-primary',
    image: STITCH_HOME_CATEGORY_GOUT_IMAGE,
    imageAlt: 'Spécialité culinaire toulousaine',
  },
  {
    label: 'Balade',
    icon: 'directions_walk',
    borderClass: 'border-sky-blue-muted',
    backgroundClass: 'bg-secondary-container/20',
    iconClass: 'text-secondary',
    image: STITCH_HOME_CATEGORY_BALADE_IMAGE,
    imageAlt: 'Rue pavée du centre historique de Toulouse',
  },
  {
    label: 'Culture',
    icon: 'museum',
    borderClass: 'border-tertiary-fixed-dim',
    backgroundClass: 'bg-tertiary-container/20',
    iconClass: 'text-tertiary',
    image: STITCH_HOME_CATEGORY_CULTURE_IMAGE,
    imageAlt: 'Détail architectural et culturel à Toulouse',
  },
];
---

<main class="md:hidden">
  <section class="relative flex min-h-[min(751px,100dvh)] flex-col justify-end overflow-hidden px-container-margin pt-20 pb-12">
    <div class="absolute inset-0 z-0">
      <img
        alt="Pont Neuf et Garonne au coucher du soleil à Toulouse"
        class="h-full w-full object-cover brightness-[0.7]"
        src={STITCH_HOME_HERO_MOBILE_IMAGE}
      />
      <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-transparent to-transparent" />
    </div>
    <div class="relative z-10 space-y-6">
      <h2 class="font-headline-xl text-headline-xl leading-tight text-white text-shadow-sm">
        Toulouse à voir, à vivre, à aimer
      </h2>
      <p class="font-body-lg text-body-lg text-white/90">
        Redécouvre la Ville Rose à travers le regard de ceux qui l'aiment vraiment.
      </p>
      <div class="flex flex-col gap-3">
        <a
          href={APP_APK_DOWNLOAD_URL}
          class="flex items-center justify-center gap-2 rounded-xl bg-deep-rose px-8 py-4 font-label-md text-label-md text-white shadow-lg transition-transform active:scale-95 no-underline"
        >
          <span class="material-symbols-outlined">download</span>
          Télécharger l'App
        </a>
        <a
          href="/catalogue"
          class="flex items-center justify-center gap-2 rounded-xl border-2 border-white/30 bg-white/10 px-8 py-4 font-label-md text-label-md text-white backdrop-blur-md transition-transform active:scale-95 no-underline"
        >
          Voir le catalogue
          <span class="material-symbols-outlined">arrow_forward</span>
        </a>
      </div>
    </div>
  </section>

  <section class="bg-surface-container-low px-container-margin py-12" id="concept">
    <div class="mb-8">
      <h3 class="font-headline-md text-headline-md text-deep-rose">Le paradoxe du choix ? On l'a résolu.</h3>
      <p class="mt-2 text-on-surface-variant">Plus besoin de scroller des heures, on a sélectionné l'essentiel.</p>
    </div>
    <div class="grid grid-cols-3 gap-3">
      {
        categoryItems.map((categoryItem) => (
          <div class="home-category-chip flex flex-col items-center gap-2">
            <div
              class:list={[
                'relative flex aspect-square w-full items-center justify-center overflow-hidden rounded-2xl border shadow-sm',
                categoryItem.backgroundClass,
                categoryItem.borderClass,
              ]}
            >
              <span class:list={['material-symbols-outlined material-symbols-filled z-10 text-3xl', categoryItem.iconClass]}>
                {categoryItem.icon}
              </span>
              <div class="absolute inset-0 opacity-20">
                <img alt={categoryItem.imageAlt} class="h-full w-full object-cover" src={categoryItem.image} loading="lazy" />
              </div>
            </div>
            <span class="font-label-md text-label-md text-on-surface">{categoryItem.label}</span>
          </div>
        ))
      }
    </div>
  </section>

  <section class="px-container-margin py-8">
    <div class="texture-paper rounded-2xl border border-white/20 bg-primary-container p-6 text-on-primary-container shadow-xl">
      <h4 class="mb-4 font-headline-md text-headline-md">Finies les hésitations</h4>
      <ul class="space-y-4">
        <li class="flex items-start gap-3">
          <span class="material-symbols-outlined rounded-lg bg-white/20 p-1">verified</span>
          <div>
            <span class="block font-bold">Testé par nous</span>
            <span class="text-sm opacity-90">Pas d'avis truqués, seulement des vraies expériences toulousaines.</span>
          </div>
        </li>
        <li class="flex items-start gap-3">
          <span class="material-symbols-outlined rounded-lg bg-white/20 p-1">schedule</span>
          <div>
            <span class="block font-bold">Gagne du temps</span>
            <span class="text-sm opacity-90">Ton trio du jour, prêt en quelques secondes.</span>
          </div>
        </li>
        <li class="flex items-start gap-3">
          <span class="material-symbols-outlined rounded-lg bg-white/20 p-1">local_offer</span>
          <div>
            <span class="block font-bold">Bons plans exclusifs</span>
            <span class="text-sm opacity-90">Des avantages chez nos artisans partenaires préférés.</span>
          </div>
        </li>
      </ul>
      <a
        href="/apropos"
        class="mt-6 block rounded-xl bg-white py-3 text-center font-label-md text-label-md font-bold text-deep-rose no-underline"
      >
        Comment ça marche ?
      </a>
    </div>
  </section>

  <section class="bg-surface px-container-margin py-12" id="catalogue">
    <div class="mb-6 flex items-end justify-between">
      <div>
        <h3 class="font-headline-md text-headline-md text-ink-black">Le cœur battant de Toulouse</h3>
        <p class="text-sm text-on-surface-variant">Les pépites du moment.</p>
      </div>
      <a href="/catalogue" class="font-label-md text-label-md text-primary no-underline">Tout voir</a>
    </div>
    <div class="home-bento-mobile">
      <a href="/catalogue" class="home-bento-item-large group relative block h-48 overflow-hidden rounded-2xl">
        <img
          alt="Bistrot toulousain authentique"
          class="h-full w-full object-cover"
          src={STITCH_HOME_BENTO_CASSOULET_IMAGE}
          loading="lazy"
        />
        <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent" />
        <div class="absolute bottom-4 left-4 text-white">
          <span class="mb-2 inline-block rounded-full bg-terra-cotta px-2 py-0.5 text-[10px] font-bold uppercase">Coup de cœur</span>
          <p class="font-bold">Chez Émile : Le vrai Cassoulet</p>
        </div>
      </a>
      <a href="/catalogue#lieux" class="group relative block h-40 overflow-hidden rounded-2xl">
        <img
          alt="Street art à Toulouse"
          class="h-full w-full object-cover"
          src={STITCH_HOME_BENTO_STREET_ART_IMAGE}
          loading="lazy"
        />
        <div class="absolute bottom-3 left-3 text-xs font-bold text-white">Art de Rue</div>
      </a>
      <a href="/catalogue#lieux" class="group relative block h-40 overflow-hidden rounded-2xl">
        <img
          alt="Cour secrète d'un hôtel particulier toulousain"
          class="h-full w-full object-cover"
          src={STITCH_HOME_BENTO_COURTYARD_IMAGE}
          loading="lazy"
        />
        <div class="absolute bottom-3 left-3 text-xs font-bold text-white">Cours Secrètes</div>
      </a>
    </div>
  </section>

  <section class="relative mx-container-margin my-12 overflow-hidden rounded-3xl border-2 border-dashed border-soft-pink bg-soft-pink/20 p-8 text-center">
    <div class="absolute top-0 right-0 -mt-12 -mr-12 h-24 w-24 rounded-full bg-soft-pink/30" />
    <h3 class="mb-4 font-headline-lg-mobile text-headline-lg-mobile text-deep-rose">Tu es un pionnier ?</h3>
    <p class="mb-6 text-on-surface-variant">
      Aide-nous à construire le guide parfait. Donne ton avis sur nos parcours et gagne des récompenses.
    </p>
    <a
      href={`mailto:${SITE_CONTACT_EMAIL}?subject=Retour%20TouRose`}
      class="inline-block rounded-full bg-deep-rose px-8 py-3 font-label-md text-label-md text-white shadow-lg transition-transform active:scale-95 no-underline"
    >
      Je participe !
    </a>
  </section>

  <section class="overflow-hidden bg-surface-container px-container-margin py-12" id="download">
    <h3 class="mb-8 text-center font-headline-md text-headline-md">Tes suggestions du jour</h3>
    <div class="relative mx-auto max-w-[280px] rounded-[3rem] border-4 border-surface-container-highest bg-ink-black p-3 shadow-2xl">
      <div class="overflow-hidden rounded-[2.5rem] bg-background">
        <div class="bg-primary px-4 pt-8 pb-4 text-white">
          <p class="text-[10px] tracking-widest uppercase opacity-80">Aujourd'hui</p>
          <h4 class="text-lg font-bold">Hello, Explorateur !</h4>
        </div>
        <div class="flex justify-center px-2 py-4">
          <AppScreenshotCarousel dotTone="light" compact />
        </div>
      </div>
      <div class="absolute top-1/4 -right-6 max-w-[120px] rotate-3 rounded-2xl border border-soft-pink bg-white p-4 shadow-xl">
        <p class="mb-1 text-[10px] font-bold text-deep-rose">Météo Tip</p>
        <p class="text-[9px] leading-tight">Prends ton chapeau, le soleil tape Place du Capitole !</p>
      </div>
    </div>
  </section>
</main>

<script>
  document.querySelectorAll('.home-category-chip').forEach((categoryChip) => {
    categoryChip.addEventListener('click', () => {
      categoryChip.classList.toggle('scale-105');
      const iconElement = categoryChip.querySelector('.material-symbols-outlined');
      if (!(iconElement instanceof HTMLElement)) return;
      iconElement.style.transition = 'transform 0.3s ease';
      iconElement.style.transform = 'rotate(10deg)';
      window.setTimeout(() => {
        iconElement.style.transform = 'rotate(0deg)';
      }, 300);
    });
  });
</script>
