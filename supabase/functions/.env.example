# Local Edge Function secrets (copy to .env — never commit real keys)
IMPORT_CRON_SECRET=local-import-secret
# OPENAGENDA_PUBLIC_KEY=
# OPENAGENDA_AGENDA_UID=

# Phase 6 — Stripe (test keys only in local dev)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# Optional alert webhook for failed/stale imports (Slack, etc.)
# IMPORT_ALERT_WEBHOOK_URL=
